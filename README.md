# Moni Front

## Installation

```bash
# change directory
cd bitcoin-web

# install dependencies
npm install

# server in localhost: 3000
npm run dev

# To start the build version
# build for production
npm run build

# start build server
npm run start

# Now you can find build folder "./next" in main directory, you can use it to deploy to production server

```
